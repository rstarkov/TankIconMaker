<UserControl x:Class="TankIconMaker.DataSourceEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TankIconMaker"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="noneTemplate">
            <TextBlock Margin="0,5,0,5" Text="None" FontWeight="Bold"/>
        </DataTemplate>
        <DataTemplate x:Key="sourceTemplate">
            <StackPanel Margin="0,5,0,5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,0,8,0"  ToolTip="{Binding Description}"/>
                    <TextBlock Text="{Binding Language}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Author:" FontWeight="Bold" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding Author}"/>
                </StackPanel>
                <TextBlock Text="{Binding Description}"/>
            </StackPanel>
        </DataTemplate>
        <local:DataSourceTemplateSelector x:Key="sourceTemplateSelector"/>
    </UserControl.Resources>
    <ComboBox Name="ctCombo" ItemTemplateSelector="{StaticResource sourceTemplateSelector}" />
</UserControl>
